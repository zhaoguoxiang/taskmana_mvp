[project]
name = "taskmana-mvp"
version = "0.1.0"
description = "Graph-based task management — tasks are nodes, links are edges."
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "fastapi>=0.139.0",
    "pydantic>=2.0",
    "sqlmodel>=0.0.39",
    "uvicorn>=0.30.0",
]
